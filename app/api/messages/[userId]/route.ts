import { env } from "cloudflare:workers";
import { getChatGPTUser } from "@/app/chatgpt-auth";

async function recipient(userId:string){return await env.DB.prepare("SELECT u.id,p.first_name AS firstName FROM users u LEFT JOIN profiles p ON p.user_id=u.id WHERE u.id=? AND u.status='active'").bind(userId).first<{id:string;firstName:string|null}>()}
export async function GET(_:Request,{params}:{params:Promise<{userId:string}>}){const identity=await getChatGPTUser();if(!identity)return Response.json({error:"Sign in is required."},{status:401});try{const{userId}=await params;const person=await recipient(userId);if(!person)return Response.json({error:"Member not found."},{status:404});const result=await env.DB.prepare("SELECT id,sender_id AS senderId,receiver_id AS receiverId,body,created_at AS createdAt FROM messages WHERE (sender_id=? AND receiver_id=?) OR (sender_id=? AND receiver_id=?) ORDER BY created_at ASC,id ASC LIMIT 300").bind(identity.userId,userId,userId,identity.userId).all();await env.DB.prepare("UPDATE messages SET read_at=CURRENT_TIMESTAMP WHERE sender_id=? AND receiver_id=? AND read_at IS NULL").bind(userId,identity.userId).run();return Response.json({person,messages:result.results,currentUserId:identity.userId});}catch{return Response.json({error:"Unable to load this conversation."},{status:500})}}
export async function POST(request:Request,{params}:{params:Promise<{userId:string}>}){const identity=await getChatGPTUser();if(!identity)return Response.json({error:"Sign in is required."},{status:401});try{const{userId}=await params;if(userId===identity.userId)return Response.json({error:"You cannot message yourself."},{status:400});const person=await recipient(userId);if(!person)return Response.json({error:"Member not found."},{status:404});const{body}=await request.json() as{body?:string};const text=typeof body==="string"?body.trim().slice(0,1200):"";if(!text)return Response.json({error:"Write a message first."},{status:400});await env.DB.prepare("INSERT INTO messages (sender_id,receiver_id,body) VALUES (?,?,?)").bind(identity.userId,userId,text).run();return Response.json({sent:true});}catch{return Response.json({error:"Unable to send this message."},{status:500})}}
