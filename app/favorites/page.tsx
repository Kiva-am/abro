"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
type Item={id:number;title:string;monthlyRent:number;roomType:string;city:string;neighborhood:string|null;description:string};
export default function FavoritesPage(){const[items,setItems]=useState<Item[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const f=await fetch("/api/favorites");const{listingIds}=await f.json() as{listingIds:number[]};const l=await fetch("/api/listings");const{listings}=await l.json() as{listings:Item[]};setItems(listings.filter((item)=>listingIds.includes(item.id)));setLoading(false);})();},[]);return <main className="market-page"><header className="site-header shell market-header"><Link className="brand" href="/"><span className="brand-logo"/></Link><nav><Link href="/listings">Browse homes</Link></nav><Link className="button button-dark" href="/listings/new">List a place</Link></header><section className="market-hero shell"><div><span className="eyebrow">YOUR SHORTLIST</span><h1>Saved listings</h1><p>Keep promising places together while you compare location, rent, and living details.</p></div></section><section className="market-results shell">{loading?<p>Loading saved homes…</p>:items.length===0?<div className="empty-state"><strong>No saved homes yet.</strong><p>Tap the heart on a listing to keep it here.</p><Link className="button button-dark" href="/listings">Browse homes</Link></div>:<div className="market-grid">{items.map((item,index)=><Link className="market-card" href={`/listings/${item.id}`} key={item.id}><div className={`market-photo listing-photo-${index%3===0?"one":index%3===1?"two":"three"}`}><span>♥ Saved</span><b>0{index+1}</b></div><div className="market-card-body"><span className="area">{item.neighborhood?`${item.neighborhood}, `:""}{item.city}</span><h3>{item.title}</h3><p>{item.description}</p><div className="market-price"><strong>{Number(item.monthlyRent).toLocaleString()} ETB<small>/month</small></strong><span>View details →</span></div></div></Link>)}</div>}</section></main>}
